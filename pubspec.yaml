name: last_fm_music_search_app
description: Basic music search app using clean architecture

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  flutter_bloc: ^7.3.0
  equatable: ^2.0.3
  bloc_test: ^8.2.0
  freezed_annotation: 0.14.3
  mocktail: ^0.1.4
  get_it: ^7.2.0
  retrofit: ^2.0.1
  google_fonts: ^2.1.0
  html: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: any

  build_runner: ^2.1.2
  freezed: ^0.14.5
  flutter_lints: ^1.0.4
  retrofit_generator: any
  flutter_launcher_icons: "^0.9.2"

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/speaker.png"

flutter:
  uses-material-design: true

  assets:
    - assets/images/speaker.png
